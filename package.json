{
  "private": true,
  "scripts": {
    "restore": "(cd backend && npm run restore) && (cd web && npm run restore)",
    "start": "concurrently --names \"BACKEND,WEB\" --prefix-colors \"blue,orange\" \"cd backend && npm run start\" \"cd web && npm run start\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
