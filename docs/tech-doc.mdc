---
description: This document contains all general development rules and guidelines for this project, applicable to all AI agents (Claude, Cursor, Codex, Gemini, etc.).
alwaysApply: true
---

# Core Principles
- **Open source or free**: Use only open source code or free code. also never use something may have costs in the future  
- **Small tasks, one at a time**: Always work in baby steps, one at a time. Never go forward more than one step.
- **Type Safety**: All code must be fully typed.
- **Clear Naming**: Use clear, descriptive names for all variables and functions.
- **Incremental Changes**: Prefer incremental, focused changes over large, complex modifications.
- **Question Assumptions**: Always question assumptions and inferences.
- **Pattern Detection**: Detect and highlight repeated code patterns.
- **No comments**: unless the logic is non-obvious.
- **Lean, DRY and Clean Code**: no over-engineering, no premature abstractions, no repeat code, human reading code
- **Config data and DB Use**: Prioritize configuration files for stable contents and DB for operative dynamic data. Do not leave text, static data or values or important things lost in the code.

# Language Standards
- **English**: All technical artifacts must always use English, including:
    - Code (variables, functions, classes, comments, error messages, log messages)
    - Documentation (README, guides, API docs)
    - Data schemas and database names
    - Configuration files and scripts
    - Git commit messages
    - Test names and descriptions
- **Italian and Spanish**: Our conversations in the chat can be in italian or spanish:
    - If I talk to you in italian you answer me in italian.
    - If I talk to you in spanish you answer me in spanish.
    - code references can be in english

# Specific standards

For detailed standards and guidelines specific for this application refer to:
- [Specific Tech Doc](./specific-tech-doc.mdc)

# Code Conventions
- Validate at the boundary before domain logic.
- Use early returns for auth/ownership checks.
- Keep responsibilities focused (no mixed transport/domain concerns).

# Security Rules
- Never commit secrets (`.env*`, tokens, keys).
- Never trust client input; always validate/sanitize.
- Restrict destructive actions to authenticated ownership/authorization.
- Keep upload handling constrained by validation (mime/size/type).

# Documentation Update Rules
When code changes, update affected docs in the same change set.

1. Identify impacted surfaces: API, auth flow, data model, tooling, security rules, standards, flows, etc..
2. Update the proper file. (doc, prompt or skill or rule)
3. Keep wording concise and executable (agent-first, no narrative).
4. If a rule change is driven by user feedback, ask approval before changing standards.

# Project Skills

- Skills live in `ai-specs/skills`.
- When a request matches a skill, load and follow the corresponding `SKILL.md` automatically before continuing.
- Also load any referenced files in the skill folder (for example, `references/*.md`) when the skill requires them.

# Spec-Driven Development with OpenSpec

**Agent Note**: This project uses OpenSpec for spec-driven development workflows.

- **OpenSpec CLI is installed** (v1.3.1+) and operational in this workspace.
- **When the user asks you to implement a feature or change**, you may use OpenSpec to structure the work:
  - `openspec new change <name>` — Create a change proposal with artifacts (proposal.md, design.md, specs/, tasks.md)
  - `openspec status --change <name>` — Check artifact completion status
  - `openspec instructions <artifact> --change <name>` — Get structured guidance for creating an artifact
  - `openspec list` / `openspec show <name>` — View active changes and specs
  - `openspec archive <name>` — Archive a completed change and merge specs into main
  - `openspec validate <name>` — Validate change structure and specs format

- **How to use it in this project**: 
  - Project config is in `openspec/config.yaml` — it references ai-specs standards and enforcement rules.
  - Changes live in `openspec/changes/` (active) and `openspec/changes/archive/` (completed).
  - Main specs live in `openspec/specs/` (reference source of truth).
  - When you propose or implement a change, follow the artifacts generated by OpenSpec.
  - The user will guide you through what to build; use OpenSpec to organize requirements, design, and tasks.

- **When NOT required**: The user may ask for changes without triggering OpenSpec workflows (e.g., quick fixes, exploration). Adapt to their request style — if they want structured workflow, use OpenSpec; if they want direct implementation, just code.